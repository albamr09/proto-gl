{
  "name": "proto-gl",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "concurrently": "^9.2.0",
    "terser": "^5.21.0",
    "tsup": "8.3.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsup",
    "build:watch": "NODE_ENV=development tsup",
    "clean": "rm -rf dist",
    "dev:setup": "pnpm build && pnpm link --global && cd example && pnpm link --global proto-gl",
    "dev:clean": "pnpm unlink --global proto-gl",
    "dev": "concurrently -k -n tsup,vite -c yellow,cyan \"tsup --watch --onSuccess 'cd example && pnpm run build:watch'\" \"cd example && pnpm run preview\"",
    "test": "tsup --config tests/tsup.config.ts && node tests/build/main.js && rm -rf tests/build"
  }
}
