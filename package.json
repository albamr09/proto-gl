{
  "name": "webgl",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "terser": "^5.21.0",
    "tsc-alias": "1.8.15",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && cd .test_build && node tests/main.js && cd .. && rm -rf .test_build"
  }
}
