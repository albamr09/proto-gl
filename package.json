{
  "name": "webgl",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@digitak/tsc-esm": "3.1.4",
    "@types/node": "^20.14.9",
    "terser": "^5.21.0",
    "tsc-alias": "1.8.15",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsc-esm",
    "clean": "rm -rf dist",
    "dev": "tsc-esm --watch",
    "test": "tsc-esm -p tsconfig.test.json && cd .test_build && node tests/main.js && cd .. && rm -rf .test_build"
  }
}
