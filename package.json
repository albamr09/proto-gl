{
  "name": "proto-gl",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "concurrently": "^9.1.2",
    "terser": "^5.21.0",
    "tsup": "8.3.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsup",
    "build:watch": "NODE_ENV=development tsup",
    "clean": "rm -rf dist",
    "dev": "tsup --watch --onSuccess \"cd example && rm -rf node_modules && pnpm install && pnpm run dev\"",
    "test": "tsup --config tests/tsup.config.ts && node tests/build/main.js && rm -rf tests/build"
  }
}
