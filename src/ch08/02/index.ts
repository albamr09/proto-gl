import { createDescriptionPanel, initGUI } from "../../lib/gui/index";

const initProgram = () => {};

const init = () => {
  initGUI();
  createDescriptionPanel(
    "On this example we will showcase how we can use unique ids in order to determine which object is selected."
  );
  // Program
  // Data
  // Render
  // Controls
};

window.onload = init;
