{
  "name": "proto-gl-examples",
  "private": true,
  "type": "module",
  "dependencies": {
    "@proto-gl": "file:.."
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "serve": "^14.2.4",
    "tsup": "8.3.0"
  },
  "scripts": {
    "build": "tsup",
    "build:watch": "NODE_ENV=development tsup",
    "dev": "concurrently \"NODE_ENV=development tsup\" \"serve dist\""
  }
}
