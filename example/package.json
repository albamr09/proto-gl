{
  "name": "proto-gl-examples",
  "private": true,
  "type": "module",
  "dependencies": {
    "@proto-gl": "file:.."
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "tsup": "8.3.0",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "build": "tsup",
    "dev": "concurrently \"NODE_ENV=development tsup\" & \"serve dist\""
  }
}
